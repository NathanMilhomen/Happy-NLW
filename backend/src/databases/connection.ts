// import { Sequelize } from 'sequelize';

// const sequelize = new Sequelize('happy', 'root', 'password', {
//     host: 'localhost',
//     dialect: 'mssql'
// });

import { createConnection } from "typeorm";

createConnection();